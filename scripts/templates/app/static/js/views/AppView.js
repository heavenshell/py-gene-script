define([
  'jquery',
  'underscore',
  'backbone'
], function ($, _, Backbone) {
  var AppView = Backbone.View.extend({
    initialize: function () {
    },
    render: function () {
    }
  });
  return AppView;
});
